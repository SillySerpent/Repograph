# JSONL trace fixtures

Sample lines follow ``repograph/runtime/trace_format.py`` (``jsonl_call_trace``).

- ``minimal_call.jsonl`` — one ``session`` line and one ``call`` line for parser checks.

Dynamic integration tests generate traces under a temporary ``.repograph/runtime/`` directory.
