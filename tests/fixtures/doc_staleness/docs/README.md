# Doc Staleness Fixture

This document references both live and stale symbols.

## Live references (should NOT generate warnings)

Use `process_data` to transform items into a result mapping.

Call `validate_input` before passing data to the processor.

The `DataProcessor` class is the main entry point.

## Moved reference section

The current implementation lives in `src/processor.py`.
